# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp
*~

# Logs / local scratch
*.log
.cache/

# Agent tooling and local skill state — not part of the site
.agents/
.impeccable/
skills-lock.json

# Brand source material. This repo is public (GitHub Pages), so the strategy
# decks and layered artwork would be downloadable by anyone. An earlier commit
# already removed the reference images for this reason.
reference/

# Jekyll build output. GitHub Pages builds from source on its own
# servers, so a committed _site would be dead weight that also masks
# whether the source actually builds.
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
vendor/

# Local scratch
screenshot.png
